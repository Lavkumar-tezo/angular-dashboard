import { HighligtErrorDirective } from './highligt-error.directive';

describe('HighligtErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligtErrorDirective();
    expect(directive).toBeTruthy();
  });
});
